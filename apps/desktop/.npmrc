# The desktop shell is an app, not a library: running `pnpm run start` must
# not re-run dependency installation (pnpm's verify-deps-before-run), which
# is slow and re-triggers postinstall hooks.
verify-deps-before-run=false
